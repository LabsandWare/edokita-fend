import { Component, OnInit } from '@angular/core';

@Component({
    selector: 'user-cmp',
    moduleId: module.id,
    templateUrl: './provider-checker.component.html'
})

export class ProviderCheckerComponent implements OnInit{
    ngOnInit(){
    }
}
